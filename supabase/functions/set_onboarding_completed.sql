
-- This is a placeholder file to document the RPC function we've created in Supabase
-- The actual function has been created using a SQL migration

-- CREATE OR REPLACE FUNCTION public.set_onboarding_completed(user_id UUID)
-- RETURNS VOID
-- LANGUAGE SQL
-- SECURITY DEFINER
-- AS $$
--   UPDATE public.user_profiles
--   SET has_completed_onboarding = true
--   WHERE id = user_id;
-- $$;
